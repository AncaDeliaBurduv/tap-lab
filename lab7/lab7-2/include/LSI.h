#ifndef LSI_H
#define LSI_H


class LSI
{
    public:
        LSI();
        ~LSI();
        LSI(const LSI& other);
        LSI& operator=(const LSI& other);

    protected:

    private:
};

#endif // LSI_H
